import {ProfileMain} from "@/patterns/ProfileMain/ProfileMain";
import { array } from "public/mockup";
export default function Profile() {

  return (
     <ProfileMain auction={array} profile/>
  );
}
